# IMPORTANT: This file contains placeholder values.
# Replace all CHANGE_ME values with real credentials before deploying.
# Do NOT commit real credentials to source control.
#
# After editing, apply with:
#   kubectl apply -f namespace.yaml
#   kubectl apply -f secrets.yaml
#   kubectl apply -f configmaps.yaml
#   kubectl apply -f icecast.yaml
#   kubectl apply -f radio.yaml
#   kubectl apply -f ingress.yaml  # optional
---
apiVersion: v1
kind: Secret
metadata:
  name: icecast-credentials
  namespace: radio-agent
type: Opaque
stringData:
  source-password: "CHANGE_ME"
  relay-password: "CHANGE_ME"
  admin-password: "CHANGE_ME"
